#Database
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/spring_web
    username: root
    password: txt1995
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true

#MailSender
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL}
    password: ${PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
#Logger
#logging:
#  file:
#    name: logs/MultiShop.log
#    path: logs
#  pattern:
#    file: '%d [%thread] %-5level %-50logger{40} : %msg%n'
#  logback:
#    rollingpolicy:
#      file-name-pattern: 'logs/achieved/MultiShop-%d{yyyy-MM-dd}-%i.log'
#      max-file-size: 5MB
